pragma radiantscript ^0.1.0;

contract RXDFungibleToken()(int a, int b) {
    if (a == 1) {
        int c = 2;
        int d = 3;
        d = d + 5;
        require(true);
        stateSeparator;
        require(c == 3);
    }
    // FIXME! these won't be OP_DROPped before OP_STATESEPARATOR!
    int d = 2;
    d = d + 2;

    function spend(int e) {
        require(b == 2);
        require(e == 3);
    }
}
